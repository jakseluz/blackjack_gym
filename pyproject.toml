[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["blackjack_env"]

[project]
name = "blackjack_env"
version = "0.0.1"
dependencies = [
  "gymnasium",
  "matplotlib",
  "pygame>=2.1.3",
  "seaborn",
  "pre-commit",
]
