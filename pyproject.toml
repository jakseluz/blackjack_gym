[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["blackjack_env"]

[project]
name = "blackjack_gym"
version = "0.1.0"
dependencies = [
  "gymnasium",
  "matplotlib",
  "pygame>=2.1.3",
  "seaborn",
  "pre-commit",
]
